  <div>
    <%= commented.content %>
    <%= link_to "編集", edit_comment_path(commented.id), method: :get, remote:true, class:"btn btn-outline-info"%>
    <%= link_to "削除", comment_path(commented.id), method: :delete, remote:true, class:"btn btn-outline-danger", data: { confirm: '本当に削除して良いですか？' } %>
  </div>
