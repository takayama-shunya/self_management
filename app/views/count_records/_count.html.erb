<div>
  <%= button_to  count_up_count_record_path(record.id), method: :post, class: 'btn ',remote: true do %>
    <span class=""><i class="fa fa-plus-square"></i></span>
  <% end %> 
  <%= record.content %><%= record.unit %>
  <%= button_to  count_down_count_record_path(record.id), method: :post, class: 'btn ',remote: true do %>
    <span class=""><i class="fa fa-minus-square"></i></span>
  <% end %> 
</div>
